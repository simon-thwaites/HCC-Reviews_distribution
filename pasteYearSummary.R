# paste year summary text

pasteYearSummary <- function(df_userSelected_seasonData){
  
  n <- nrow(df_userSelected_seasonData())
  
  
  textlast <- paste(cat("Number of responses: \t", n, "\n\n"))
  
  # textlast <- paste(cat("--- \n"))
  
}